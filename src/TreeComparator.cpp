#include "TreeComparator.h"
#include "ParseTree.h"

using namespace std;

TreeComparator::TreeComparator(unordered_map<int, int> scores):scores(scores){

}

int TreeComparator::compare(const ParseTree& t1, const ParseTree& t2){
    return 0;
}